function square(num) { // function normal
    return num * num;
}

const square = (num) => { // funcion con arrow function
    return num * num;
}

const square = num => num * num; // funcion arrow function con return explicito